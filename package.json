{
  "name": "ec-csv-preflight",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first EC product CSV import preflight MVP",
  "type": "module",
  "scripts": {
    "qa": "node tests/qa.mjs",
    "check": "node --check app.js && node --check src/csv-engine.mjs && node --check tests/qa.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
