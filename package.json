{
  "name": "ec-csv-preflight",
  "version": "0.3.0",
  "private": true,
  "description": "Local-first Shopify product CSV preflight and safe correction delivery engine",
  "type": "module",
  "scripts": {
    "qa": "node tests/qa.mjs",
    "check": "node --check app.js && node --check src/csv-engine.mjs && node --check src/csv-fixer.mjs && node --check src/delivery-pack.mjs && node --check tests/qa.mjs",
    "verify": "npm run check && npm run qa"
  },
  "engines": {
    "node": ">=20"
  }
}
